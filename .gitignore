# Ferramentas locais / artefatos de IA / docs internos
.claude/
COPY-CATALOGO.md
screenshots/

# Comuns
node_modules/
.env
.env.local
*.log

# Uploads em runtime (imagens enviadas + .secrets.json) — NUNCA versionar.
# O Dockerfile recria a pasta no build; em produção é um volume persistente.
uploads/

# Clone vazio criado pelo GitHub Desktop — ignorar
areamembros/

# lixo do Finder
.DS_Store

# servidor de teste local (header do arquivo manda nao commitar)
scratch-fake-bunny.js
